import { Meteor } from 'meteor/meteor'

import '../imports/api/Funcionarios'
import '../imports/api/Clientes'
import '../imports/api/Servico'
import '../imports/api/Frota'

Meteor.startup(() => {
  // code to run on server at startup
})
